# 天地：使用说明

适用：Minecraft 1.20.1 Forge；随 `invisible_hand-1.3.0.jar` 提供。

「天地」是独立能力，不需要选择无形之手，也不从神秘商人购买。管理员执行 `/tiandi grant <玩家>` 授予；使用 `/tiandi revoke <玩家>` 撤销并清除该玩家的所有门。授予状态在死亡、换维度、重新登录和服务器重启后保留。

## 操作

| 默认按键 | 功能 |
| --- | --- |
| J | 进入或退出绘制模式，默认要求主手为空 |
| G | 绘制中切换天门、地门 |
| 潜行 + J | 绘制中打开门名称输入框 |
| 右键方块表面 | 选取该表面相邻空气格，依次标记两个内部对角点 |
| H | 绘制中销毁准星指向的、属于自己的门 |

按键均可在游戏控制设置中修改。绘制中仍可移动和跳跃，但不能攻击、挖掘、使用物品或打开容器；受到伤害、离线、换维度或 30 秒超时后会退出绘制。

## 画出一组门

1. 搭一个完整的黑曜石框，内部保留 **2×3** 空气空间（最小外框为 4×5，四角也需要框架）。
2. 空手按 J，以天门模式开始。右键框架内缘，使选中的相邻空气格落在内部左下角；再标记内部右上角。
3. 第一个角点会显示线框。预览绿色表示形状、尺寸、空气空间和框架通过本地检查；服务端仍会重新检查区域保护和位置。
4. 到出口位置准备另一副框架，按 G 切换为地门，再画两个角点。两道门使用同一名称，即可自动配对。

默认名称为 `default`。名称允许英文字母、数字、下划线，最长 16 个字符；统一按小写处理，非法名称回退为 `default`。不同玩家的同名门互不关联。同一玩家画出的同名同类型新门会替换旧门，另一端不受影响。

门既可以垂直，也可以水平。垂直门内部宽度为 2—21、高度为 3—21；水平门按 X 方向 2—21、Z 方向 3—21 计算。**地门的出口方向朝向绘制时玩家所在的一侧**；要从天花板向下释放实体，应站在平面下侧画地门。

## 通行规则

- 天门接收实体，地门只负责输出。没有配对地门时，天门不会传送。
- 当前支持同维度的玩家、生物、箭矢、三叉戟、末影珍珠、雪球、药水、掉落物、TNT、船和普通坐骑。
- 保留速度大小，方向调整为地门正面；投射物保留射手归属，TNT 保留引信。
- 默认天门两面都可进入；传送后有 10 tick 的防重复传送保护。
- 门框尺寸必须容纳实体，出口有实体碰撞或方块阻挡时暂不传送。
- 门存续期间会保持相关区块加载，删除时释放加载票。

## 生命周期和配置

默认每种类型最多 3 道门，创建间隔 5 秒。

- **天门**：存在 120 秒，到期或服务器关闭时清除；需要时可以重新绘制同名天门，连接已有地门。
- **地门**：永久作为传送点保留，玩家离线、区块重新加载和正常服务器重启后仍然有效；保存所属玩家、名称、维度、范围与出口朝向。
- **整门破坏**：默认框架要求开启时，任意一块黑曜石框架（包括四角）损坏，最多 1 秒内整片门面消失；门内任意传送门方块被移除时，立即清除整片门面。其余黑曜石框架保留。
- 主动销毁、同名同类型替换或撤销能力也会清除地门，并删除存档记录，不会在重启后复活。默认死亡不销毁已有门；开启 `portalDestroyOnDeath` 后两种门都会随死亡清除。

配置位于 `config/invisible_hand-common.toml`：

| 配置项 | 默认值 |
| --- | --- |
| `portalDurationTicks`（仅天门寿命，地门永久） | 2400 |
| `portalCreateCooldownTicks` | 100 |
| `portalTeleportCooldownTicks` | 10 |
| `portalDrawTimeoutTicks` | 600 |
| `portalMaxPerType` | 3 |
| `portalRequireFrame` | true |
| `portalFrameBlocks` | `["minecraft:obsidian"]` |
| `portalRequireEmptyHand` | true |
| `portalBothSideTrigger` | true |
| `portalDestroyOnDeath` | false |

管理员还可以使用 `/tiandi list [玩家]`、`/tiandi destroyall [玩家]`，以及 `/tiandi destroy <玩家> <名称> heaven/earth` 管理门。

跨维度、通行刷新天门寿命、超额自动删除旧门仍属于未加入的可选扩展。地门已支持随世界存档保存。当前视觉使用蓝色/橙色半透明门面和粒子。

扩展能力图鉴会根据窗口高度分页，可用鼠标滚轮、底部箭头或 Page Up / Page Down 浏览。进入说明页后返回，会停留在之前的列表页。
