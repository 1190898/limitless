# 时间删除 · 使用说明

适用：Minecraft 1.20.1 Forge，`invisible_hand-1.3.0.jar`。此能力仍由无形之手的神秘商人出售。

## 共同效果

- 滤镜开启时，立即显示范围内生物的发光轮廓，并叠加明亮的金色透视标记框。标记框在滤镜之后绘制，可隔着地形看见。
- 范围默认以施法者为中心、半径 16 格，随施法者移动。其他玩家也会被标记、定身并获得黑暗效果。
- 定身会暂停生物行动，并由服务器锁定位置，阻止移动、跳跃、击退位移和攻击。神秘商人延续原有的时间免疫，只被标记。
- 施法者可以移动、放置方块、喝药水；时间删除期间不能攻击生物。
- 目标离开范围后恢复，进入范围后受到影响。能力结束、死亡、退出或跨维度时清理状态；多人范围重叠时，最后一份影响解除后才恢复。
- 此次增加的发光、黑暗在解除时清除，原有的同类效果保留。不会永久修改目标的 AI、移动属性或重力。

## 主动：绘制诱导轨迹

1. 按 **O** 发动时间删除，默认持续 **10 秒**，冷却 **5 分钟**。
2. 在能力持续期间，对准 **16 格内地面的上表面**，按独立的能力操作键 **I** 添加一个诱导点。
3. 依次添加更多点，明亮的末地烛粒子会连接成路线。最多 **12 点**，相邻点距离为 **0.5～16 格**；落点须有站立空间。
4. **潜行 + I** 清空路线，可重新绘制。
5. 时间恢复后，能力期间曾锁定你的生物会优先按顺序前往这些点，期间暂时停止攻击和重新索敌。默认最长 **5 秒**，走完后提前结束，随后恢复正常索敌。

无路线时正常恢复。诱导不控制其他玩家，也不会把原本中立的旁观生物强行变成追击者。生物使用自身寻路方式移动，不会沿路线穿墙或被强制传送；某一点连续 2 秒没有前进时会跳至下一点，最多受诱导总时长限制。

**O 只负责主动发动，I 只负责能力生效期间的操作。** 诱导操作不检查主动的 5 分钟冷却。两键均可在“控制 → 按键绑定 → 无形之手”中分别修改；屏幕提示显示实际绑定的操作键。

## 被动：受击后的短距瞬移

- 受到生物或其投射物的有效攻击时，自动触发默认 **2 秒**的时间删除，独立冷却 **10 秒**。触发它的那次伤害仍会结算。
- 在被动持续期间按 **I**，向当前视线方向瞬移，默认最远 **6 格**；每次至少间隔 **0.4 秒**，可在窗口内多次使用。主动能力仍处于冷却时照常可用，瞬移只检查自身的短间隔和被动剩余时长。
- 瞬移沿完整玩家体积检查障碍：遇到墙、低矮顶棚、液体、世界边界或未加载区域会提前停止，不会穿进墙里。
- 起点、终点有传送粒子，沿途有明亮粒子轨迹。朝空中使用可以落在空中，瞬移后仍会正常下落。
- 被动结束后瞬移权限立即消失，再按 I 也不会发动主动能力。被动期间按 O 不会消耗瞬移次数、刷新持续时间或改变冷却。屏幕提示会区分当前是主动诱导还是被动瞬移；提示中的按键会跟随控制设置。

## 配置

配置文件：游戏目录 `config/invisible_hand-common.toml`。已有配置会保留，新增选项自动补充；修改后重启游戏或服务器最稳妥。

| 配置项 | 默认值 | 作用 |
| --- | ---: | --- |
| `timeEraseDurationTicks` | 200 | 主动持续 10 秒 |
| `timeEraseCooldownTicks` | 6000 | 主动冷却 5 分钟 |
| `timeEraseRadius` | 16 | 定身与标记半径 |
| `timeErasePassiveDurationTicks` | 40 | 被动持续 2 秒，也是瞬移可用窗口 |
| `timeErasePassiveCooldownTicks` | 200 | 被动冷却 10 秒 |
| `timeEraseLureDurationTicks` | 100 | 时间恢复后最多诱导 5 秒 |
| `timeEraseBlinkDistance` | 6 | 每次瞬移的最远距离 |
| `timeEraseBlinkCooldownTicks` | 8 | 两次瞬移间隔 0.4 秒 |

20 tick = 1 秒，按服务器正常运行速度计算。

## 更新与验证

客户端和服务器均需替换为本次同一份 `invisible_hand-1.3.0.jar`，因为能力操作使用了独立的同步消息，协议更新为 13。版本号保持 1.3.0。

2026-09-26：构建成功，全部 **22 项服务器 GameTest 通过**，其中7 项时间删除测试，另 15 项覆盖此前的天地门、炼金术等功能。新增测试覆盖即时发光与黑暗、行动和击退锁定、原有效果恢复、多人重叠及玩家位置锁定、实际诱导寻路与恢复索敌、猪灵的行为记忆和仇恨恢复、瞬移碰撞和冷却、离开范围及停服清理。另增按键与冷却分离回归测试：验证主动长冷却中的设点/清空、被动连续瞬移、主动键不消耗瞬移，以及会话结束后操作键不会误发动技能。图鉴已同步添加以上操作说明。

已检查 54 个 JSON 文件，中英文本各 350 个键一致。输出 JAR 为 422082 字节，与构建产物校验一致，未打包测试类或测试场景。

SHA-256：`B825064FFD7B05C0862381A919748C0429CF26DCF0C8DCA0FFB355405847C46B`。

服务器测试无法代替客户端画面验收；实际滤镜、透视标记和粒子观感仍需在游戏内确认，尤其是使用其他光影或渲染模组时。


